//! Custom, reusable UI widgets — as opposed to `panels/`, which is
//! app-specific chrome built out of them (plus egui's own built-ins).
//! Currently just `editor`, the highlighted/squiggled text-editing widget.

pub mod editor;
