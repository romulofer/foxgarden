//! Cross-cutting presentation: the selectable editor font (`fonts`), the
//! UI's icon glyphs (`icons`), the light/dark color palette (`theme`), the
//! indentation style (`indent`), and editor display toggles (`view`).

pub mod fonts;
pub mod icons;
pub mod indent;
pub mod theme;
pub mod view;
